Requires Visual Studio.

Open src/wolf.sln
The "Release Dedicated" configuration is to build rtcwPub server only. "Release" is to build the Wolf SE client.
Once a build config is chosen, then open the Build menu at the top and choose "Build Solution".